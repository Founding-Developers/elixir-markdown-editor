/**
 * ElixirMarkdownEditor JS hooks.
 *
 * Import and register these hooks with your LiveSocket:
 *
 *   import { FileTreeHook } from "path/to/elixir_markdown_editor/assets/js/index.js";
 *
 *   let liveSocket = new LiveSocket("/live", Socket, {
 *     hooks: { ...otherHooks, FileTreeHook }
 *   });
 */
export { FileTreeHook } from "./file_tree_hook.js";
